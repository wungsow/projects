import { InlineLoadingDirective } from './inline-loading.directive';

describe('LoadingDirective', () => {
  it('should create an instance', () => {
    const directive = new InlineLoadingDirective();
    expect(directive).toBeTruthy();
  });
});
